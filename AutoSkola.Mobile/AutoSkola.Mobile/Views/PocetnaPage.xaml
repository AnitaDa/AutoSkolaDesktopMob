<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AutoSkola.Mobile.Views.PocetnaPage">
    <ContentPage.Content>
        <StackLayout>
            <!--<ScrollView>
            --><!--<ListView ItemsSource="{Binding top5Instruktora}" SelectedItem="{Binding odabraniInstruktor}" ItemSelected="ListView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell >
                            <StackLayout  BackgroundColor="PowderBlue" Padding="10">
                                <Label Text="{Binding ImePrezime}"
                                    TextColor="DodgerBlue"
                                       LineBreakMode="NoWrap"
                                       Style="{DynamicResource ListItemTextStyle}" ></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>--><!--
            </ListView>
            </ScrollView>-->

        </StackLayout>
    </ContentPage.Content>
</ContentPage>